import Decimal from "decimal.js";
import c_add from "./components/add.js";
import c_sub from "./components/sub.js";

export const add = c_add;

export const sub = c_sub;

export const test = (a, b) => {
  console.log("test");
};
